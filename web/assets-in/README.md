# assets-in

Drop the finished Affinity pieces here, named as in `docs/affinity-brief.md` (`opengraph-image.jpg`, `logo-wordmark.svg`, `glyph-ideas.svg` and so on).
The `reference/` folder holds the starting files (marble still, current logo, palette); it is not committed.
