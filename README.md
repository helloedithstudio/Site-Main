# edith

Website for **edith**, a club of builders that runs itself.

- `web/` the site (Next.js App Router, React, three.js, GSAP, Lenis). See `web/README.md`.
- `blender/` the Blender sources and scripts for the 3D objects used on the site.

Run the site: `cd web && npm install && npm run dev`.
